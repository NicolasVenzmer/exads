import "./Header.css"
import Logo from "../Logo/Logo";

function Header() {
    return (
        <header className="header-container">
            <Logo/>
        </header>
    )
}

export default Header;